apiVersion: v1
kind: Service
metadata:
  name: flask-app
  namespace: jenkins-lab
spec:
  type: NodePort
  selector:
    app: flask-app
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
