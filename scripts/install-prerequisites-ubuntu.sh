#!/usr/bin/env bash
set -Eeuo pipefail
echo 'Install Docker Engine + Compose V2, kind, kubectl, git, curl, jq and make using their official instructions.'
echo 'This helper intentionally does not run unattended privileged package installation.'
